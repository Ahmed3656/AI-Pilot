# DealPilot Egypt MVP Acceptance Matrix

This matrix is the verification index for `docs/mvp-contract.md` version 1.0.0. `Automated` means a deterministic test is required in the owning component; `Manual` means an explicitly observed acceptance step is also required. Mocks and local HTML fixtures are valid only where stated.

The last column records the original pre-merge baseline and is retained as historical audit context; it is not the current implementation status. Current automated evidence is `npm run check:docker`, `npm run mvp:smoke`, and the deterministic `npm run demo` integration journey. Physical-phone/live-merchant rows remain manual and must not be claimed from the test adapter.

## Scope and public API

| ID       | Requirement                                                                               | Future verification                                                                                            | Type               | Owner               | Original pre-merge baseline                                         |
| -------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------- | ------------------------------------------------------------------- |
| SCOPE-01 | Market, currency, timezone are exactly `EG`, `EGP`, `Africa/Cairo`                        | Contract-schema test; API DTO rejection tests for supplied country/currency/timezone; Compose config assertion | Automated          | API, Infra          | Partial; API fixes EG/EGP but timezone contract is absent           |
| SCOPE-02 | Locales are exactly `ar-EG` and `en-EG`                                                   | API create-run validation tests and mobile locale/type tests                                                   | Automated          | API, Mobile         | Gap; API omits locale and AI uses `ar`/`en`                         |
| SCOPE-03 | Request categories are `auto`, `retail`, `food`, `cinema`; resolved category never `auto` | Schema drift test, API e2e create tests, AI classifier tests                                                   | Automated          | API, AI             | Gap; API rejects `auto`                                             |
| SCOPE-04 | No country selector or international configuration                                        | Mobile rendered-screen test; `rg`/config smoke assertion against forbidden public fields/variables             | Automated + Manual | Mobile, Integration | Mobile UI appears Egypt-only; end-to-end contract unverified        |
| API-01   | Only `/api/v1` is the public prefix                                                       | API e2e verifies every canonical route and verifies `/v1/...` returns 404; gateway smoke test                  | Automated          | API, Infra          | Gap; API e2e currently uses `/v1` while mobile uses `/api/v1`       |
| API-02   | Create/get run DTOs exactly match contract                                                | OpenAPI drift test plus API e2e snapshots with unknown-field rejection                                         | Automated          | API                 | Gap; current `query/category` and response fields differ            |
| API-03   | Merchant catalog is Egypt/category constrained                                            | Unit test catalog constants and e2e category filtering                                                         | Automated          | API                 | Partial; fixed catalog exists                                       |
| API-04   | Clarification endpoint uses current request ID and rejects stale answers                  | API e2e for success, stale ID, unknown question, duplicate idempotency key                                     | Automated          | API                 | Gap; public endpoint absent                                         |
| API-05   | Domain approval accepts a non-empty approved subset                                       | API unit/e2e subset, superset, lookalike, duplicate, and stale-request cases                                   | Automated          | API                 | Partial; API subset works, AI currently requires the full set       |
| API-06   | Address grant is request-, domain-, run-, and TTL-scoped                                  | API e2e with fake clock and vault inspection                                                                   | Automated          | API                 | Partial; request ID and full contract are absent                    |
| API-07   | Seat-hold approval is tied to request, offer, and approved domain                         | API e2e for valid, stale, wrong offer/domain/category, duplicate cases                                         | Automated          | API                 | Partial; request ID absent                                          |
| API-08   | Pause/resume/cancel/complete use canonical control endpoint and semantics                 | State-machine unit tests and API e2e for every valid/invalid action                                            | Automated          | API                 | Partial; state advances before AI acceptance                        |
| API-09   | Claim/renew/release are explicit exclusive-lease endpoints                                | API concurrency/e2e tests with fake clock and two users                                                        | Automated          | API                 | Gap; existing takeover is an action and has no lease                |
| API-10   | Viewer tokens are created by POST and never returned in a URL                             | API e2e plus log-capture assertion                                                                             | Automated          | API                 | Gap; current endpoint is GET and query-token WebSocket auth is used |
| API-11   | Event history endpoint supports cursor and bounded page                                   | Repository and API e2e tests for ordering, pagination, unknown/pruned cursor                                   | Automated          | API                 | Gap; current GET is only a 426 placeholder                          |
| API-12   | Report endpoint returns the exact report DTO during and after a run                       | API e2e response-schema validation with complete, partial, and empty fixtures                                  | Automated          | API                 | Gap; current report shape differs                                   |
| API-13   | All POST operations enforce 24-hour idempotency semantics                                 | Shared API e2e table: identical replay and changed-body conflict                                               | Automated          | API                 | Gap                                                                 |
| API-14   | Run ownership failures do not reveal existence                                            | Two-user API e2e returns 404 for non-owner                                                                     | Automated          | API                 | Gap; shopping controller is currently public                        |

## State, errors, events, and internal integration

| ID       | Requirement                                                                                                                   | Future verification                                                                   | Type      | Owner           | Original pre-merge baseline                                           |
| -------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | --------- | --------------- | --------------------------------------------------------------------- |
| STATE-01 | Run status enum and allowed transitions are exact                                                                             | Table-driven state-machine unit test generated from contract artifact                 | Automated | API             | Partial; enum mostly matches but paused can resume to too many states |
| STATE-02 | `resumeStatus` is recorded and is the only resume target                                                                      | State-machine and persistence tests across every pausable status                      | Automated | API             | Partial; fallback behavior permits ambiguity                          |
| STATE-03 | Terminal states are immutable                                                                                                 | Unit/e2e attempts every event/control from each terminal status                       | Automated | API             | Partial                                                               |
| ERR-01   | Every API/internal failure uses the canonical error envelope                                                                  | Global-filter unit test and e2e table for 400/401/403/404/409/410/426/429/502/503/500 | Automated | API, AI         | Gap; current Nest and FastAPI envelopes differ                        |
| ERR-02   | Error output contains no secret, address, token, query string, or upstream body                                               | Redaction property tests and captured-log/error snapshot tests                        | Automated | API, AI         | Partial; existing redaction is not contract-complete                  |
| EVT-01   | Every event has exactly `id`, `runId`, `type`, `status`, `timestamp`, `payload`                                               | JSON-schema validation for stored history and every WebSocket frame                   | Automated | API             | Gap; current frames use `eventId`/`observedAt` and omit status        |
| EVT-02   | Event names and payloads are the frozen set                                                                                   | Producer/consumer contract tests using shared fixtures                                | Automated | API, AI, Mobile | Gap; existing event vocabulary differs                                |
| EVT-03   | Duplicate event IDs are idempotent; conflicting content is rejected                                                           | Repository/API e2e duplicate and collision cases                                      | Automated | API             | Partial; identical/different duplicates are not distinguished         |
| EVT-04   | Reconnect replays without gaps and clients deduplicate by ID                                                                  | API WebSocket integration test with injected disconnect; mobile hook test             | Automated | API, Mobile     | Gap; no server replay/history exists                                  |
| EVT-05   | Pruned cursors produce reset event and close 4009                                                                             | WebSocket integration test with fake retention clock                                  | Automated | API, Mobile     | Gap                                                                   |
| INT-01   | Internal paths and camelCase DTOs are exact                                                                                   | API↔AI fake-server contract tests against OpenAPI components                          | Automated | API, AI         | Partial; paths mostly exist but DTOs/IDs differ                       |
| INT-02   | Commands are only `clarify`, `pause`, `resume`, `cancel`, `complete`, `approve_domains`, `grant_address`, `approve_seat_hold` | Enum drift tests in both services and negative alias tests                            | Automated | API, AI         | Gap; API emits `pause_ai`/`resume_ai`; AI accepts extra aliases       |
| INT-03   | API and AI share one run ID                                                                                                   | Fake AI create/command e2e and persistence assertion                                  | Automated | API, AI         | Gap; API stores a separate `aiRunId`                                  |
| INT-04   | Internal create/commands/events are idempotent for 24 hours                                                                   | AI and API tests with duplicate IDs, changed bodies, and timeout/retry                | Automated | API, AI         | Gap                                                                   |
| INT-05   | API status/effective grants do not advance when AI rejects or times out                                                       | Fake AI 409/500/timeout e2e for every mutation                                        | Automated | API             | Gap; current service transitions before command                       |
| INT-06   | Invalid AI events cannot bypass approval or state rules                                                                       | API e2e adversarial event table                                                       | Automated | API             | Partial                                                               |

## Browser lifecycle, control, and safety

| ID      | Requirement                                                                               | Future verification                                                                                 | Type               | Owner                  | Original pre-merge baseline                                            |
| ------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------ | ---------------------- | ---------------------------------------------------------------------- |
| LIFE-01 | Exactly one browser session is created per approved merchant and workers overlap in time  | Fake WebDriver call-count/concurrency test through selection, partial failure, pause, and handoff   | Automated          | AI                     | Covered by manager worker lifecycle tests                              |
| LIFE-02 | Same session/cookies survive `ready_for_handoff` and `user_takeover`                      | Fake session-ID integration test; physical-phone observation of a harmless prepared session         | Automated + Manual | AI, Infra, Integration | Gap; AI currently closes browser in `_execute` finally block           |
| LIFE-03 | Browser closes only on terminal state or absolute TTL                                     | Fake-clock lifecycle tests covering all statuses and cleanup artifacts                              | Automated          | AI, API                | Gap                                                                    |
| LIFE-04 | Only the AI-requested merchant can receive one controller; all AI workers pause first     | Target-mismatch API e2e plus action-lock/focused-browser integration test                           | Automated          | API, AI                | Gap                                                                    |
| LIFE-05 | Lease renew/release/expiry and automatic recovery follow contract                         | Fake-clock API↔AI integration test including rejected resume and TTL fallback                       | Automated          | API, AI                | Gap                                                                    |
| LIFE-06 | Selenium timeout is long enough for handoff and ports stay private                        | `docker compose config` assertions plus network reachability smoke test                             | Automated          | Infra                  | Gap; current Selenium timeout is 900 seconds                           |
| SAFE-01 | Pre-navigation and post-redirect URLs require approved registrable root                   | AI unit tests for subdomains, lookalikes, credentials, ports, redirects, popups, tab switches       | Automated          | AI                     | Partial; core URL guard exists but complete interception is unverified |
| SAFE-02 | Unexpected domain, CAPTCHA, login/OTP, prompt injection, and browser warning pause safely | Local HTML fixture tests; no bypass behavior                                                        | Automated          | AI                     | Partial                                                                |
| SAFE-03 | AI cannot enter/inspect payment fields or perform final actions in Arabic/English         | DOM/coordinate/keyboard/script action tests against local fixtures, including false-positive review | Automated          | AI                     | Partial                                                                |
| SAFE-04 | Seat selection that creates a hold requires fresh approval                                | Cinema fixture integration test with action-call assertion                                          | Automated          | AI, API                | Partial                                                                |
| SAFE-05 | Human alone can perform payment/final action in same paused session                       | Physical-phone manual checklist; verify AI action log ends before final control                     | Manual             | Integration            | Pending; must never be automated                                       |
| SAFE-06 | Mocks/recordings are never represented as live                                            | UI environment banner test and demo-script checklist                                                | Automated + Manual | Mobile, Integration    | Gap                                                                    |

## Address, report, environment, and deployment

| ID      | Requirement                                                                               | Future verification                                                                  | Type      | Owner              | Original pre-merge baseline                                 |
| ------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | --------- | ------------------ | ----------------------------------------------------------- |
| ADDR-01 | Plaintext address is process-memory only and expires at earliest bound                    | API vault fake-clock tests and persistence inspection                                | Automated | API                | Partial; 30-minute vault exists                             |
| ADDR-02 | AI resolves one semantic field for the active approved recipient domain                   | API↔AI tests for every field, wrong run/domain, expired/revoked grant                | Automated | API, AI            | Partial                                                     |
| ADDR-03 | Address values/variants are absent from logs, events, reports, screenshots, model inputs  | Canary-address end-to-end fixture with byte/string scan of all artifacts             | Automated | API, AI, Mobile    | Partial; current tests cover only response/report strings   |
| RPT-01  | Merchant attempts include outcomes, timing, evidence, and partial failures                | Report schema tests with success/block/timeout/safety fixtures                       | Automated | API, AI            | Gap                                                         |
| RPT-02  | Valid, excluded, and incomplete offers are separate and evidence-linked                   | Report builder unit tests for each validity rule                                     | Automated | API, AI            | Gap                                                         |
| RPT-03  | Money uses two-decimal strings and exact total formula                                    | Decimal arithmetic property/table tests, rounding and double-count prevention        | Automated | API, AI, Mobile    | Gap; current API uses JSON numbers                          |
| RPT-04  | Every coupon links source/result evidence and has canonical rejection reason              | Coupon fixture tests for every status/reason                                         | Automated | AI, API            | Gap; current AI emits empty evidence arrays                 |
| RPT-05  | Winner requires two valid offers and uses only scope-qualified conclusion                 | Ranking/report tests with 0, 1, 2+, tie, and partial-failure cases                   | Automated | AI, API            | Partial; ranking exists but report contract is not enforced |
| RPT-06  | Food tip is zero/excluded and cinema booking fees are mandatory                           | Category-specific report fixture tests                                               | Automated | AI, API, Mobile    | Partial                                                     |
| ENV-01  | Only canonical environment names are accepted and readiness enforces live secrets         | API/AI settings tests; Compose config scan rejects deprecated names/default secrets  | Automated | API, AI, Infra     | Gap; Compose uses several deprecated/non-consumed names     |
| ENV-02  | No country/currency/timezone configuration exists; fixed `TZ` is Cairo                    | Compose config assertion and application config scan                                 | Automated | Infra, Integration | Gap                                                         |
| ENV-03  | Mobile appends `/api/v1` to an origin-only base URL and requires auth live                | Mobile config unit/build-time tests                                                  | Automated | Mobile             | Partial; path usage is canonical but auth defaults false    |
| ENV-04  | Caddy routes canonical HTTP/WebSocket paths and never exposes AI/WebDriver/noVNC directly | Caddy adapter test and external network smoke test                                   | Automated | Infra              | Gap; current WebSocket matcher targets a different path     |
| ENV-05  | Viewer/internal tokens never appear in URLs or access logs                                | Gateway/API log-capture smoke test                                                   | Automated | Infra, API         | Gap; current WebSocket contract uses query token            |
| DEMO-01 | Automated tests use mocks/fixtures and never contact live checkout                        | Test network denylist/sandbox assertion and source scan for live merchant calls      | Automated | Integration        | Pending integration guard                                   |
| DEMO-02 | Live preflight is read-only and stops before login/payment/order/booking                  | Human checklist with timestamped, redacted outcome; no private screenshots committed | Manual    | Integration        | Pending; not part of this contract task                     |
| DEMO-03 | Physical phone receives HTTPS/WSS and takes over the exact live session                   | Manual device checklist records run ID/session fingerprint and release recovery      | Manual    | Infra, Integration | Pending                                                     |

## Current integration evidence

- Contract/OpenAPI drift, forbidden aliases, canonical route and environment-name checks: `npm run test:contract` and `npm run test:infra`.
- API state, DTO, idempotency, ownership, event, report, auth, and rejected-command behavior: API unit/e2e suite.
- AI command vocabulary, browser safety/lifecycle, partial failure, report ordering, and deterministic adapter behavior: Python suite.
- Mobile canonical service paths, contract parsing, UI flows, and WebSocket reconnection behavior: mobile Jest suite.
- Real Compose migration, API -> AI -> parallel Selenium workers -> API persistence, live/replayed events, retained-session claim/release/resume, and log privacy: `npm run demo`.
- Manual evidence is still required for real merchant behavior and a physical phone over HTTPS/WSS. The deterministic adapter cannot satisfy those rows.

## Contract merge gate

Before this branch is merged and component work begins:

- `docs/mvp-contract.md`, this matrix, the OpenAPI projection, and the drift test must agree.
- Root formatting, lint, typecheck, unit/e2e tests, and builds must run. Any skipped validation is a blocker and must be reported.
- `docker compose -f infra/phase1/docker-compose.yml config` must run when Docker/Compose is available; unavailability must be reported as a blocker rather than silently skipped.
- The diff must contain no API keys, real tokens, addresses, cookies, private screenshots, or other private data.
- The commit must be pushed on a `codex/*` branch before Tasks 1-4 use it as their base.
